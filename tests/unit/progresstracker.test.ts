import React from 'react';
import { ProgressTracker } from '@/components/verify/ProgressTracker';

describe('ProgressTracker Component', () => {
  it('exports ProgressTracker correctly', () => {
    expect(ProgressTracker).toBeDefined();
  });
});
